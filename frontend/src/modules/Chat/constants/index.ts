interface Actions {
  EXPLORE: string;
  SUMMARIZE: string;
}

export const ACTIONS: Actions = {
  EXPLORE: '#explore',
  SUMMARIZE: '#summarize',
};
export const ACTION_VALUES = [ACTIONS.EXPLORE, ACTIONS.SUMMARIZE];
